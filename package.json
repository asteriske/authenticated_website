{
  "name": "lambda-functions",
  "version": "1.0.0",
  "description": "AWS Lambda functions",
  "scripts": {
    "test": "jest",
    "build": "./build/package-lambda.sh"
  },
  "dependencies": {
    "aws-jwt-verify": "^4.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
